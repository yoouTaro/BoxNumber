  <h1>
        {boxNumber(22)
          ?.split(",")
          .filter((item) => !!item)
          .map((_, i) => {
            return (
              <Image
                src={_}
                style={{ height: 40, width: 40 }}
                preview={false}
              />
            );
          })}
      </h1>
